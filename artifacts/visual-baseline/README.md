# Baseline visual — Fase 0F

Capturas deterministas del modo de simulación. No contienen datos productivos. Toda solicitud que no apunta al servidor Vite local se bloquea antes de salir del navegador.

Regenerar: `npm run visual:baseline`

Verificar artefactos: `npm run visual:baseline:check`
